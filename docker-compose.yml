rdb1:
  build: .
  ports:
    - 8080:8080
  volumes:
    - ./data/rdb01:/data

rdb2:
  build: .
  links:
    - rdb1
  environment:
    JOIN: rdb1
  ports:
    - 8081:8080
  volumes:
    - ./data/rdb02:/data

rdb3:
  build: .
  links:
    - rdb1
  environment:
    JOIN: rdb1
  ports:
    - 8082:8080
  volumes:
    - ./data/rdb03:/data
